@model EMS.Service.ViewModels.Group.GroupViewModel

@{
    ViewData["Title"] = "Details";
}

<div class="container">
    <h1 class="mt-4">Group Details</h1>

    <div>
        <h4>GroupViewModel</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                Id
            </dt>
            <dd class="col-sm-10">
                @Model.Id
            </dd>
            <dt class="col-sm-2">
                Name
            </dt>
            <dd class="col-sm-10">
                @Model.Name
            </dd>
            <dt class="col-sm-2">
                Scientific
            </dt>
            <dd class="col-sm-10">
                @Model.Scientific
            </dd>
        </dl>
    </div>

    <div class="mt-3">
        <a asp-action="Edit" class="btn btn-warning">Edit</a> |
        <a asp-action="Index" class="btn btn-primary">Back to List</a>
    </div>
</div>
