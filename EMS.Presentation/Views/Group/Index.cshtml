@model IEnumerable<EMS.Service.ViewModels.Group.GroupViewModel>

@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <h1 class="mt-4">Group Index</h1>

    <p>
        <a asp-action="Create" class="btn btn-primary">Create New</a>
    </p>

    <table class="table">
        <thead>
            <tr>
                <th>
                    Id
                </th>
                <th>
                    Name
                </th>
                <th>
                    Scientific
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Id
                    </td>
                    <td>
                        @item.Name
                    </td>
                    <td>
                        @item.Scientific
                    </td>
                    <td>
                        <a asp-action="Edit" class="btn btn-warning">Edit</a> |
                        <a asp-action="Details" class="btn btn-info">Details</a> |
                        <a asp-action="Delete" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
